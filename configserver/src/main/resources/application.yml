spring:
  application:
    name: configserver
  profiles:
    #active: native
    active: git
  cloud:
    config:
      server:
        #native:
          #search-locations: classpath:/config
          #search-locations: file:///D://Personal//Study//Projects//config
        git:
          uri: "https://github.com/prashant4805/config-server.git"
          default-label: main
          timeout: 5
          clone-on-start: true
          force-pull: true

encrypt:
  key: "45ECQW5KAL1985FG"

server:
  port: 8071
