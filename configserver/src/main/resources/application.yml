spring:
  application:
    name: configserver
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          #search-locations: classpath:/config
          search-locations: file:///D://Personal//Study//Projects//config
server:
  port: 8071
